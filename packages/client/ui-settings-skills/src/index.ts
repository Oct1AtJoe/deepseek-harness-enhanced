/** Host loader entry for the skills-tab browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the skills management tab. */
export function apply(): void {}
