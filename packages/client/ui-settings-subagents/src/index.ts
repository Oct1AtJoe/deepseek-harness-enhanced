/** Host loader entry for the subagents-tab browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the subagents management tab. */
export function apply(): void {}
